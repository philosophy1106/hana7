<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hana7.springdemo.dao.PostRepository">
    <resultMap id="postWithUser" type="Post">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="writer" column="writer"/>
        <result property="createdAt" column="createdAt"/>

        <association property="user" javaType="User">
            <id property="id" column="id"/>
            <result property="name" column="name"/>
            <result property="email" column="email"/>
            <result property="mobile" column="mobile"/>
        </association>
    </resultMap>

    <insert id="insert" parameterType="Post" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into Post(title, writer, content) values(#{title}, #{writer}, #{content})
    </insert>

    <select id="getPost" resultMap="postWithUser">
        select p.*, u.name, u.email, u.mobile
        from Post p inner join User u on p.writer = u.id
        where p.id = #{id}
    </select>

    <select id="getPostx" resultType="Post">
        select * from Post where id = #{id}
    </select>

    <select id="getUser" parameterType="Post" resultType="User">
        select * from User where id = #{writer}
    </select>
    <select id="getUsers" resultType="User">
        select u.*
        from User u inner join Post P
                               on u.id = p.writer
    </select>

    <select id="getPosts" resultType="Post">
        select * from Post
    </select>

    <update id="update" parameterType="Post">
        update Post
        set title = #{title}, content = #{content}
        where id = #{id}
    </update>

    <delete id="delete" parameterType="Integer">
        delete from Post
        where id = #{id}
    </delete>
</mapper>